Tyler Degen

-description of interpreter
The interpreter is built off of a singular ParseTree design. Each token is a ParseTree (instead of being an object).
-description of ParseTree class
The ParseTree class, simply, has its type, the list of its children, and its value.
-brief user manual
To use the ParseTree as currently implemented, simply compile all the Java files and run the following command:
java ptt testfile.txt
-Brief test plan / remainig bugs
I could not figure out how to build while loops with my parsetree. As such, I was unable to figure out how to print or 
Additionally, there seems to be an issue with my program reading multiple line programs. If they are all on the same line, as included in test.txt, it works better.
